# Maintainer (yuan_modu): Atila Satilmis <atila.satilmis@gmail.com>

pkgname=docker-alpine-nginx
pkgver=r110.b01b4f0
pkgrel=1
pkgdesc="Containerized Alpine nginx"
arch=('any')
license=('custom')
url="http://www.example.com"
depends=('docker')
makedepends=('git')
source=("$pkgname::git+https://github.com/YuanModu/Dockerfile.git")
md5sums=('SKIP')

pkgver() {
  cd "$srcdir/$pkgname"
  printf "r%s.%s" "$(git rev-list --count HEAD)" "$(git rev-parse --short HEAD)"
}

package() {
  cd "$srcdir/$pkgname"
  install -Dm644 nginx/docker-alpine-nginx@.service \
    "$pkgdir/usr/lib/systemd/system/docker-alpine-nginx@.service"
}
